#include <stdio.h>
 
int main() {

    int A, B, X;

    //ler as entradas

    scanf("%d", &A);
    scanf("%d", &B);

    //calcular soma
    X = A + B;

    //exibir o resultado
    printf("X = %d\n", X);

    return 0;
}